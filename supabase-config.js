/* ------------------------------------------------------------------
   cammap - supabase project config

   Two values, nothing else. Plain browser JavaScript, no build step,
   loaded with a plain <script> tag before account.js so the two
   globals below exist by the time account.js runs.

   Both of these are the PUBLIC values for the project - see SETUP.md
   for exactly where to find them (Project Settings -> API) and why
   they are safe to commit. The service_role key is a different,
   secret value and must never be pasted in here or anywhere else in
   this repository.

   Replace the two placeholders below with your project's real
   values. Until you do, account sign-in will fail - the site keeps
   working as a plain map either way.
   ------------------------------------------------------------------ */

var SUPABASE_URL = "https://hdwdefswcnkzlvmyqybw.supabase.co";
var SUPABASE_ANON_KEY = "sb_publishable_ZC5aCGJcmfMqC3qMNJ2rxg_ogaOyE1r";
